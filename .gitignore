# Gradle
.gradle/
build/
app/build/
# custom build dir name that iCloud Drive won't sync (see root build.gradle.kts)
build.nosync/
app/build.nosync/

# Android Studio / IntelliJ
.idea/
*.iml
local.properties
captures/

# macOS
.DS_Store

# Keystores — never commit signing keys
*.jks
*.keystore

# Kotlin
.kotlin/
tools/bbb_720p.mov

# Owner's private automation folder — contains users.json with real addon
# tokens and passwords. NEVER commit anything under it (CLAUDE.md security).
docs/reference/StremioSurfer/
docs/reference/MastaP/
# Loose secrets drops (e.g. an exported users.json) sometimes land directly
# in docs/reference/ — never commit any of them.
docs/reference/*.json
# Exported AIOStreams/AIOMetadata configs land in per-person subfolders and
# embed real service tokens (RD keys, MediaFusion links). NEVER commit.
docs/reference/Adam/
docs/reference/myles/
.DS_Store

# Owner's AIOStreams/AIOMetadata config templates — will hold real service
# tokens once populated. NEVER commit (CLAUDE.md security).
templates/
